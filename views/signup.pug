extends layout

block content
  h1= title
  p
    | The init button generates an integral&nbsp;
    code key
    |  and a&nbsp;
    code publicKey
    |  such that&nbsp;
    code publicKey = generator
      sup key
    |  for some group generated by&nbsp;
    code generator
    |.
  p
   |
   code publicKey
   |  is stored in a file&nbsp;
   code verify.txt
   |.
  form#formReg(action='/signup' method='POST')
    input(type='submit', value='init')

  p#pubInit
  p#pubKey
  p#init
  p#key
